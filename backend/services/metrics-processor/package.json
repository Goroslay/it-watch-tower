{
  "name": "metrics-processor",
  "version": "0.0.1",
  "description": "Processes metrics from NATS and writes to VictoriaMetrics",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "tsc",
    "test": "node --test -r ts-node/register src/services/cardinalityLimiter.test.ts src/validators/metricsValidator.test.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "nats": "^2.18.0",
    "axios": "^1.6.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "ts-node": "^10.0.0"
  }
}
